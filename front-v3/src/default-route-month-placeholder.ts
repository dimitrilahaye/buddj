/**
 * Id utilisé dans l’URL quand on ouvre `/budgets` sans segment mois, avant que le store ait une liste API.
 * Doit rester aligné avec la logique « mois par défaut » du routeur.
 */
export const DEFAULT_ROUTE_MONTH_PLACEHOLDER_ID = '2024-04';
